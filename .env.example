# Google Maps Embed API — "Maps Demo Key strictly for prototyping".
# Criar no Google Cloud Console (conta Samais): APIs & Services → Credentials
# → Create API Key → restringir a "Maps Embed API" + HTTP referrers
# (*.vercel.app/* e domínio próprio). Sem a chave o app usa o embed público.
VITE_GOOGLE_MAPS_API_KEY=
